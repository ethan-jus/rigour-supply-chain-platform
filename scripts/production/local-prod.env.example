# 仅含路径和开关，无密码。source 后仅影响随后启动的子进程。
export RIGOUR_LOCAL_SECRETS_ENABLED=true
export RIGOUR_LOCAL_SECRETS_DIR=/etc/rigour/local-secrets
# 每次启动时明确指定 gateway / iam / integration / crm / erp / order /
# sales / ai / bi / hr / foundation，不在公共文件固定服务名。
