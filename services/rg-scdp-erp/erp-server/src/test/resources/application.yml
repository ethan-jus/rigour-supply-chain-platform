spring:
  datasource:
    url: jdbc:h2:mem:erp_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false

rigour:
  erp:
    product-media:
      url-ttl: 10m
      cos:
        object-prefix: product-images
        region: ap-test
        bucket: rigour-erp-test-1250000000
        secret-id: test-secret-id
        secret-key: test-secret-key
  context:
    trust:
      active-key-id: v1
      keys-base64:
        v1: dGVzdC1vbmx5LXRydXN0LWtleS1tdXN0LWJlLWxvbmc=
