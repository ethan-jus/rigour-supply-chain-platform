server:
  port: 26891

spring:
  application:
    name: rigour-channel-agent-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    com.rigour: INFO
