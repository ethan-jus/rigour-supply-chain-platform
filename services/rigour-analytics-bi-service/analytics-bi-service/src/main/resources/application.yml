server:
  port: 26888

spring:
  application:
    name: rigour-analytics-bi-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    com.rigour: INFO
