server:
  port: 8082

spring:
  application:
    name: rigour-integration-migration-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    com.rigour: INFO
