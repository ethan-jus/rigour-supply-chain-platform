server:
  port: 26889

spring:
  application:
    name: rigour-hr-payroll-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

logging:
  level:
    com.rigour: INFO
