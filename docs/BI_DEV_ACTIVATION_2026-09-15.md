# BI 真实数据接入与 DEV 生效记录

> 本文保留当时验收记录。2026-09-16 本地 dev 合并后的租户字典、数据权限、迁移编号和 BI 来源接口以[兼容检查记录](DEV_SYNC_SUPPLY_SETTINGS_2026-09-16.md)为准；历史直接读库配置已被领域 API 替代。

核验日期：2026-09-15；范围：本地服务连接共享 DEV，指定测试租户。本文更新前一轮审计中的“授权、迁移、重启待完成”状态，不代表生产已发布。

## 已完成

- 用户已在已有管理员连接执行授权；BI 应用账号实测可读取 HR 员工/岗位、Sales 提交/门店四张表所需列。
- 使用各领域迁移账号执行 Flyway：HR V1 → V2、Settings V28 → V29、BI V13 → V16；未修改已执行迁移、未执行 repair。
- 按既有 `dev,local` 配置重启本地 HR（26889）、Settings（26892）、BI（26888），三个服务健康检查均为 UP。服务继续使用原 DEV 连接和环境变量，未变更密码或认证规则。
- 字典已核实：ACTIVE=在职、LEFT=离职、INACTIVE=停用、PENDING=待确认、GRAIN=颗。HR 有明确离职证据的旧 INACTIVE 已迁移为 LEFT。
- 已登录 Portal 执行 BI 全链路刷新；运行记录 213 于北京时间 11:42:15 完成 SUCCESS。随后既有定时刷新也正常完成。
- 新增“客户属性”入口及 API `/api/v1/analytics/supply/dashboard/customer-attributes`，展示客户来源、经营类别的当前客户数、期间下单客户数、订单数、应收及同批订单累计回款。
- 客户属性随 CRM_CUSTOMER 任务在同一事务中全量替换，包含历史补录、属性清空和删除；普通查询只读 BI 投影，不跨业务库实时查询。
- 新接口沿用 BI 城市/本人数据范围；订单与客户分别收紧范围、先按客户聚合，避免多订单放大客户数。未同步、空档案、源字段缺失、接口失败分别展示。

## 实际核对与页面验收

| 项目 | DEV 结果 | 验证 |
|---|---|---|
| 员工档案 | 155 人；在职 104、离职 51 | HR 与 BI 按状态逐组一致，已登录员工统计页面显示一致 |
| 城市建联 | 9910 条已提交未删除拜访，5511 家去重门店，17 个城市 | Sales 与 BI 按城市比较提交数和去重数全部一致；无未关联城市记录 |
| 客户属性 | 1721 户；来源有值 1721，经营类别有值 67 | 源字段与 BI 逐客户比较差异为 0，已登录客户属性页面正常展示 |
| 客户来源原文 | 来源为“销售订单表”“门店信息库” | 按真实原文展示，注明导入表名不代表获客渠道 |
| 订单关联限制 | 属性页汇总已关联当前 CRM 客户的订单 | 不包含缺 CRM 关联的订单，不要求与全部订单总额相等 |

城市建联最终口径：所选期间内，Sales `status=SUBMITTED`、`deletion_state=NONE`、提交时间非空，按门店去重并归入门店当前城市；不要求微信截图。审核异常仍按用户指定提交口径计入，并单独展示审核分类。

客户属性窗口按日期、城市、员工查询；客户数反映当前档案总体，日期仅约束订单。窗口明确提示客户类型、商品分类、订单来源均为全部，不静默复用不支持的筛选。

## 仍需业务配合的真实信息

1. **Sales 人员与 HR 员工对应关系**：用户随后授权按唯一同名、同城、状态一致建立固定编码关联；103 名已落库，剩余 23 名无同名 HR、5 名状态冲突。新增 BI 读取授权、重启、刷新及页面销售筛选验收均已完成；运行 218/219 成功，逐记录关联核对无差异，详见[关联实施记录](SALES_BI_BUSINESS_LINKS_2026-09-15.md)。
2. **Sales 门店与 CRM 客户对应关系**：用户授权门店参照处理，568 家已按双方唯一同名同城、无已有电话/地址冲突落库。其余按无匹配、重名或字段冲突进入复核；不能直接认定无匹配即未转客户。固定编码投影和页面关联数量已生效；13:35 快照为 9,921 次有效拜访、5,516 家去重门店，其中 252 家被拜访门店关联 CRM。拜访到下单转化指标尚未新增，不影响城市拜访去重总数。
3. **CRM 分类与账期**：1721 户未设置客户类型，账期政策表为 0。需按客户编码提供客户类型、合同账期天数及生效范围。当前 BI 缺政策时沿用既有 30 天默认值；不能当成真实合同账期，当前政策变化还会重算历史订单到期日。
4. **档案完整性**：1654 户缺经营类别；HR 有 1 人缺入职日期、155 人未填写部门。按需要的经营类别和部门统计补源档案；未知值保持缺失。
5. 若还要核对 SKU/库存/采购/退货全链路，须另按原审计补订单 SKU/单位关联和真实单据；此次未构造这些业务数据，也未把尚未接入的库存流水/退货数量指标宣称完成。

## 验证与代码状态

- Platform 根目录 `./mvnw verify` 成功，失败/错误为 0；197 项跳过（含 Docker 依赖），跳过不算通过。
- Portal 23 个 BI 测试文件、206 项测试通过；构建（含类型检查）和相关 ESLint 通过。已有构建大包提示仍保留。
- 新增客户属性隔离库测试实际执行刷新/聚合 SQL，覆盖零订单客户、客户去重、租户/城市/员工范围、取消和跨期订单排除、来源清空与删除。
- 已登录页面验证员工统计、城市建联图表和客户属性统计；源库与 BI 的实际核对结果如上。普通用户数据范围另有自动化测试，未冒充不同真实角色逐账号验收。
- 新建项目文件已暂存；既有无关修改保留。未提交、未推送、未部署生产。
