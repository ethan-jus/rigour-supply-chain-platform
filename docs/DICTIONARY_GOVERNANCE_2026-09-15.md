# 字典治理实施记录（2026-09-15）

## 结果与范围

- 在 `feature/system-optimization` 完成 Platform / Portal 的字典治理；未提交、未推送。
- DEV Settings 已从 V29 迁移至 V32，Integration 从 V22 至 V23；本地对应服务已重启且 health=UP。
- 32 组同字典、同父级、同名重复，涉及 11 个字典、64 个条目；保留标准项，将另一编码改为历史别名。
- “营业”归并到“营业中”；两条连锁规模旧编码改为新 `STORE_CHAIN_SIZE` 标准项的兼容别名。
- 用户补充确认两组业务线同义后，V32 将“鹰眼”归并到“鹰眼业务”、“零售城市代理商”归并到“城市零售代理”。沿用目标现有编码，旧名称、旧编码保留；`BUSINESS_LINE` 为 8 个标准项、2 个历史兼容项。
- 当前 92 个字典、377 个标准项、37 个历史兼容项。同父级同名标准项重复为 0，无悬空别名或别名链。
- `STORE_STATUS` 显示 6 个标准选项，保留 5 个历史编码。`STORE_SCALE` 更名“门店面积”，新建“门店连锁规模”，区分平方米与连锁门店数。

## 后续行为

- Settings：标准项、历史别名及标准编码解析。新增、编辑受同父级同名数据库约束保护。
- Integration：按租户、来源系统、来源表/对象、字段及原始值记录映射；同范围重复值先去重，10,000 条相同观察只查写一次映射；已知值解析标准项，未知值进入待映射，不再自动新增 AUTO 标准项。
- Portal：标准项 / 历史兼容 / 来源映射三个入口。支持合并预览、填写原因、版本校验和审计；保存后刷新已加载的业务字典缓存。
- 合并预览中的引用数仅统计 Settings 内子项、别名引用。它不是全系统业务引用统计，也不会自动改写业务事实。
- 来源映射用于导入阶段的字典观察与解析，保留原始值；订单、库存等业务状态仍由领域规则处理，不能把任意字典配置解释为状态机变更。
- 旧编码仍能显示当前标准名称，新下拉选项只列标准项。已部署旧客户端的响应兼容保留，等所有调用方升级后再移除旧统计字段和客户端解析回退。

## DEV 实测

- 迁移前备份：`/tmp/dictionary-governance-backup-20260915.json`，权限 0600，含原字典数据和 BI 事实校验值；不得加入 Git。
- 核对全部旧项名称、编码、父级、创建信息、备注等保留；旧业务数据未物理删除。
- 6 个业务库（CRM / ERP / Order / HR / Sales / BI），126 张表、361 个结构化 code/status/unit/type 字段未发现本轮 33 个归并旧编码引用；此检查不涵盖 JSON 和自由文本。
- 迁移完成时，三张 BI 事实表全行校验值与备份相同。后续定时刷新于北京时间 16:28:45 至 16:28:53 成功执行，全行校验值随快照变化，因此补做源库逐行复核：2,255 张订单、11,604 条明细、1,178 条回款无缺失、无多余，数量、金额、付款方式及所检查状态等关键字段差异为 0；未将全行哈希变化直接当成业务金额变化。复核记录：`/tmp/dictionary-bi-business-verify-20260915.json`。
- 同名标准项的事务内试写触发唯一约束，已回滚，无测试项残留。
- 已登录页面验证门店状态标准项及历史兼容项数量。
- 本租户 Integration 中现存飞书原始记录为 2 批、8,215 行，均为订单及明细。扫描 `STORE_STATUS` 得到 0 个来源值是该扫描范围的实际结果，不代表 CRM 没有客户数据。
- 扫描 `PAYMENT_METHOD` 得到 5 条来源映射，全部可解析；已保存“月结 → MONTHLY”的明确映射并验证落库；再次扫描仍为 5 条，手动映射保持不变、版本仍为 2。
- 配置页选择范围为完整的 92 个字典，不受主列表筛选限制。扫描结果常驻页面；到达扫描上限时提示覆盖范围可能不全，持久化异常不报告成功。
- 扫描最多取最近 100 批、每批 100,000 行；历史回扫当前覆盖飞书批次原始表，订货宝新增同步通过同一映射服务记录观察值。

## 验证

- 根目录 `./mvnw verify`：1283 项，失败 0、错误 0、跳过 199（Docker 不可用，不能视为这些集成测试通过）。
- Portal：2 个测试文件、11 项通过；`vue-tsc --noEmit -p tsconfig.app.json`、相关 eslint 和 Vite 构建通过。
- 构建日志：`/tmp/dictionary-governance-verify.log`、`/tmp/dictionary-portal-{tests,typecheck,lint,build}.log`。
- DEV 迁移/进程日志：`/tmp/dictionary-governance-{settings,integration}-{migration,runtime}.log`；日志和凭证不入库。

## 业务线同义追加处理

- 已确认业务口径并应用 V32；原 V30 / V31 迁移未改写。已登录 Portal 查询确认业务线显示 8 项，“鹰眼业务”和“城市零售代理”各保留一个标准选项。
- 迁移前事务预演通过并回滚：10 行原条目全部保留，仅 2 行增加标准项引用；无子项或已有别名引用待转移。
- 再查 6 个业务库、126 张表、361 个结构化编码/状态/单位/类型字段：这 4 个业务线编码引用为 0；不涵盖 JSON 和自由文本。原始文本继续保留。
- 备份 `/tmp/dictionary-business-line-backup-20260915.json`（0600）；迁移日志 `/tmp/dictionary-business-line-flyway-20260915.log`。
- 根目录 `./mvnw verify` 再次通过：1283 项，失败 0、错误 0、跳过 199；日志 `/tmp/dictionary-business-line-verify-20260915.log`。本次仅数据迁移与记录更新，无前端代码修改。

## 尚需业务口径确认

- 付款方式来源确实存在“A类商家 / B类商家”，保留原值及当前旧标准项。需要先确认是否表示账期/客户分层，再按领域契约拆分，不能直接映射为现金、月结等含义。
- 本轮解决已确认重复及继续自动生成重复项的问题，不将所有历史 AUTO 编码任意重命名，也不宣称全系统每个枚举的业务含义均已确认。
