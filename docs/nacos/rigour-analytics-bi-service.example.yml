# Nacos Data ID: rigour-analytics-bi-service.yaml
# Namespace: dev / 3aa03547-8948-4254-bd94-47c630db128b
# 密码只从本机 Secret、IDEA 环境变量或部署平台注入，不要替换为明文。
spring:
  datasource:
    url: jdbc:mysql://82.157.4.176:13306/rigour_bi?useUnicode=true&characterEncoding=UTF-8&connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true&sslMode=PREFERRED&allowPublicKeyRetrieval=true
    username: rigour_bi_app
    password: ${BI_DB_APP_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 2
      maximum-pool-size: 10
      connection-timeout: 30000
      validation-timeout: 5000
  flyway:
    # BI 已有正式迁移，启动时应校验并执行新增版本。
    enabled: true
    url: jdbc:mysql://82.157.4.176:13306/rigour_bi?useUnicode=true&characterEncoding=UTF-8&connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true&sslMode=PREFERRED&allowPublicKeyRetrieval=true
    user: rigour_bi_migrator
    password: ${BI_DB_MIGRATOR_PASSWORD}
    baseline-on-migrate: false
    validate-on-migrate: true
    clean-disabled: true
    locations: classpath:db/migration

rigour:
  context:
    trust:
      active-key-id: v1
      keys-base64:
        v1: ${RIGOUR_CONTEXT_TRUST_KEY_V1:}
