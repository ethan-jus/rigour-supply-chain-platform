# Sales 人员、门店固定编码关联

范围：共享 DEV；2026-09-15。用户已确认采用名称匹配建立初始业务关联，并授权门店参照处理。没有提交、推送或生产发布。

## 已落库

| 对象 | 源记录 | 已关联 | 待核对 |
|---|---:|---:|---|
| Sales 人员 → HR 员工编码 | 131 | 103 | 23 无同名 HR；5 在离职状态冲突 |
| Sales 门店 → CRM 客户 ID、编码 | 10,642 | 568 | 9,464 无同名同城客户；553 条 Sales 重名；47 条 CRM 候选不唯一；8 地址冲突；2 电话冲突 |

- 人员：仅去首尾空白，名称双方唯一、城市相同、在离职状态一致。未知状态不自动关联。
- 门店：名称、城市精确相同且双方唯一，双方启用；电话均有值而不同、CRM 地址与 Sales 已有地址均不一致时进入复核。不做模糊匹配、去分店名、去城市后缀或电话猜测。
- 568 家仅 2 家有一致电话佐证，其余主要依据名称和城市；没有一致地址佐证。规则匹配不是实地身份核验。
- 已有关联以固定编码保留；后续改名、调城、离职不自动重新匹配。目标已被占用时拒绝新增关系；数据库同时约束源、目标唯一。
- Sales 自有 `temp_sales_checkin_employee_link`、`temp_sales_checkin_customer_link` 保存匹配规则、来源摘要、名称/城市快照、批次、操作者和时间。未写 HR/CRM 档案、未建立 IAM 登录/角色绑定。
- 实测落库 103 个不同员工编码、568 个不同客户 ID/编码；复跑预览新增候选均为 0。

## BI 对接与生效状态

- Flyway：Sales V26 → V27、BI V16 → V17，均已成功；未修改历史迁移、未执行 repair。
- 刷新代码将提交人的 HR 编码、门店 CRM 客户 ID/编码投影到 `bi_sales_contact_fact`；普通查询继续只读 BI。
- 个人筛选按**拜访提交人**的固定员工编码统计，不按 CRM 当前客户负责人替代；沿用现有可信租户/城市/员工范围。未关联人员的拜访只进入其可见城市总体，不计入任何个人业绩。
- 城市总数仍按 Sales 门店去重，不因缺 CRM 对应而排除；表格增加“已关联 CRM 门店”“未关联 CRM 门店”。旧快照未带关联时显示待同步，个人查询不退回城市总数。
- 12:11 左右源库核对：9,911 次有效拜访、5,511 家去重门店；8,924 次拜访已关联员工，涉及 84 名有拜访记录的员工；252 家被拜访门店已关联 CRM。与此前 9,910 次相比增加一次真实提交，门店去重数未变。
- **已生效（13:35）**：用户已执行[三条最小授权 SQL](BI_SALES_BUSINESS_LINK_GRANTS.sql)，BI 应用账号三项读取均实测通过。本地 BI 26888 已按原 `dev,local` 配置重启并健康检查 UP；没有变更认证规则或其他领域服务。
- 已登录 Portal 触发全链路刷新，运行 218 于 13:35:26 完成 SUCCESS；随后运行 219 于 13:35:51 完成 SUCCESS。关联快照时间为 13:35:50，`business_links_ready=1`。
- 13:38 只读一致性事务核对：源库与 BI 均为 **9,921 次有效拜访、5,516 家去重门店、8,934 次已关联员工拜访、84 名有已关联拜访的员工、252 家已关联 CRM 的被拜访门店**。源端单独记录、BI 单独记录、逐记录编码/城市/审核状态/时间差异均为 0；17 个城市、109 个员工编码与城市分组（含未关联人员分组）的汇总差异均为 0。
- 拜访数和去重门店数较上一轮增加来自真实新提交。568 家已关联门店中仅 252 家在当前拜访事实中出现，其余不凭空计入建联；城市总体仍保留 5,264 家尚未关联 CRM 的被拜访门店。

## 可复跑维护程序

程序：`services/rigour-sales-work-service/tools/business_links.py`，依赖 PyMySQL。它是按需运行的 Sales 数据维护工具，不是在线请求或自动调度任务；HR/CRM 各用独立只读事务，Sales 只写自己的两张关联表。

通过环境注入 `SALES_DB_HOST/PORT/NAME/USER/PASSWORD`、`HR_DB_*`、`CRM_DB_*`。账号分别使用对应领域账号，不使用 root 或跨域通用写账号。密钥和审核文件不得进入 Git。

```bash
python3 services/rigour-sales-work-service/tools/business_links.py \
  --tenant-id "$LINK_TENANT_ID" --report /tmp/sales-link-preview.json

python3 services/rigour-sales-work-service/tools/business_links.py \
  --tenant-id "$LINK_TENANT_ID" --report /tmp/sales-link-applied.json \
  --apply --expected-digest "$LINK_PREVIEW_DIGEST" --actor "$LINK_OPERATOR"
```

apply 重新读取源数据与已有关联，完整摘要必须与预览一致；使用租户维护锁、Sales 行锁和单库事务。新增冲突直接失败回滚，不使用覆盖更新。HR/CRM 各自读取快照，跨库并非全局同一时刻；证据保留首次核对时的字段摘要，后续目标删除/修正仍需业务核对。

**未来变化**：已关联对象的新拜访及门店、员工编码关联随 BI 定时/手工刷新投影，不需改代码；新人员/新门店须再次运行预览及关联维护，源数据补齐不会绕过复核自动创建关系。此处只补编码与城市建联个人筛选，未新增拜访到下单转化指标。

## 需要业务处理

1. 人员：核实 5 条状态冲突；另外 23 名补 HR 档案或提供明确员工编码。
2. 门店：核实重名和电话/地址冲突。9,464 条未找到精确对应不能直接认定为“新客户”或“未成交”；可能是未建档、别名、城市文本差异。按真实情况补 CRM 或提供明确编码。
3. 反馈格式：`Sales 人员 ID → HR 员工编码`、`Sales 门店 ID → CRM 客户编码`。关联更正需要显式维护，不会因名称变动自动覆盖。
4. 私有核对文件放在 `/tmp/bi-business-link-review-20260915.html`，可筛选/搜索全部已关联和待处理记录；包含业务名称与编号，不进入 Git。

## 验证

- 匹配规则 11 项 Python 测试通过：状态/城市冲突、双方重名、禁止模糊匹配、电话/地址冲突、已有关联稳定、目标占用、未知状态和预览摘要变化。
- Platform 根目录 `./mvnw verify` 成功；测试报告 1,277 项，其中 197 项跳过，失败/错误为 0；跳过不计为通过。BI 测试覆盖提交/删除口径、固定员工筛选、跨租户关联隔离、未关联记录保留、CRM 覆盖数及旧快照不返回个人数据。
- Portal 本轮修正一处仍提示“不支持销售筛选”的旧口径文案；城市建联与口径 10 项测试、构建（含类型检查）、相关 ESLint 通过。构建保留既有大包提示。
- 已登录城市经营页面验证全部 17 城市及“已关联 CRM 门店 / 未关联 CRM 门店”列，并目视检查明细展示。抽查一名已关联员工，页面为长沙 93 家去重门店、10 家已关联 CRM、83 家未关联，与 BI 查询一致，其余城市为 0。此为租户管理员选择员工筛选的验收，不代替全部真实角色逐账号验收；本人权限仍由既有后端数据范围测试覆盖。
- HR 状态、CRM 来源/经营类别投影复核一致，Sales 未关联城市记录为 0。最终页面已恢复全部销售视图。新文件已暂存，未提交、未推送、未发布生产。
